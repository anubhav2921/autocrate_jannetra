{"build": {"builder": "NIXPACKS", "nixpacksPlan": {"phases": {"setup": {"nixPkgs": ["python311"], "aptPkgs": ["gcc"]}, "install": {"cmds": ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -r backend/requirements.txt"]}}, "start": {"cmd": ". /opt/venv/bin/activate && cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"}}}}
