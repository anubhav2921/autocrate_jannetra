import asyncio
from datetime import datetime
import uuid

def seed_db():
    print("[SEED] The seed database script has been disabled for MongoDB migration.")
    print("[SEED] The app will now operate primarily off of live scraped data via APScheduler.")

if __name__ == "__main__":
    seed_db()
